import 'package:flutter/material.dart';

class PlaceAAdd extends StatelessWidget {
  const PlaceAAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
